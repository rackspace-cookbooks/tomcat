site :opscode

metadata

cookbook "rackspace_java", github: "rackspace-cookbooks/rackspace_java"
cookbook "openssl"

group :integration do
  cookbook "rackspace_apt", github: "rackspace-cookbook/rackspace_apt"
end
